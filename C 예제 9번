#include <stdio.h>

int main()
{
	int a = 0;
	
	while (a < 32 || a > 126){
	scanf("%d", &a);
	if(a < 32 || a > 126){
		printf("다시 입력하세요 : ");
	}
	}
	printf("DEC : %d\n", a);
	printf(" : %c\n", a);
	return 0;
}
